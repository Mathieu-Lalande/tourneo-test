{
  "name": "tourneo-test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "mkdir -p tmp && node --loader ts-node/esm --test --test-reporter=junit > tmp/test-results.xml"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  }
}
